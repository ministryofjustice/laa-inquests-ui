apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "laa-inquests-internal-ui.fullname" . }}-test-connection"
  labels:
    {{- include "laa-inquests-internal-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "laa-inquests-internal-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
